# ffmpeg_kit_flutter — vendored fork (CUiT / komune-mobile)

Salinan `ffmpeg_kit_flutter` **6.0.3** yang sebelumnya hidup sebagai path dependency di
`komune-mobile/plugins/ffmpeg_kit_flutter`. Dipindah ke repo sendiri supaya bisa dipakai
sebagai git dependency di `pubspec.yaml`.

- Upstream: https://github.com/arthenica/ffmpeg-kit
- Import awal: dari `komune-mobile` (riwayat modifikasi lokal ada di repo itu).

Bump versi = commit di sini lalu tag baru (`v6.0.3-cuit.N`), dan update `ref:`
di `pubspec.yaml` komune-mobile.
